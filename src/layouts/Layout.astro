<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<!-- Favicon: use repo base-aware URL -->
		{(() => {
			const base = import.meta.env.BASE_URL.endsWith('/') ? import.meta.env.BASE_URL : `${import.meta.env.BASE_URL}/`;
			const pngHref = `${base}z.png`;
			const v = 'v=2'; // cache bust to override old favicon
			const png32 = `${base}z.png?${v}`;
			return (
				<>
					<link rel="icon" type="image/png" href={png32} sizes="32x32" />
					<link rel="shortcut icon" type="image/png" href={png32} />
					<link rel="apple-touch-icon" href={png32} />
				</>
			);
		})()}
		<meta name="generator" content={Astro.generator} />
		<title>BLACKZorro's Mars Station</title>
	</head>
	<body>
		<slot />
	</body>
</html>

<style>
	html,
	body {
		margin: 0;
		width: 100%;
		height: 100%;
	}
</style>
